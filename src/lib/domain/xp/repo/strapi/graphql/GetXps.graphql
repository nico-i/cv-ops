query GetXps($locale: I18NLocaleCode!) {
  xps(locale: $locale) {
    data {
      id
      attributes {
        locale
        position
        company
        info
        start
        end
        url
      }
    }
  }
}
