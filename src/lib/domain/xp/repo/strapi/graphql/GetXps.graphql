query GetXps($locale: I18NLocaleCode!) {
  xps(locale: $locale, sort: ["end","start:desc"]) {
    data {
      id
      attributes {
        locale
        position
        company
        info
        start
        end
        url
      }
    }
  }
}
