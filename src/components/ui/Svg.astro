---
export type Props = {
  className?: string;
  svgHtml: string;
};

const { className = "w-4 h-max-4 inline-block", svgHtml }: Props = Astro.props;
---

<span class={className} set:html={svgHtml} />
